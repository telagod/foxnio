# Cargo 1.94 兼容性修复记录

**更新时间**: 2026-04-04
**口径**: 本文档是历史修复记录，不代表当前项目编译状态

## 现状

这份文档原本记录的是某一轮针对 Cargo / Rust 版本兼容性的排障过程。

它现在只能作为历史参考，不能继续当作当前仓库状态判断依据。原因很简单：

- 原文里的错误数量、修复进度、分支名已经过期
- 当前 backend 编译状态已经发生变化
- 当前项目状态应以 `docs/CURRENT_STATUS.md` 与实际 `cargo check` 结果为准

## 已完成

可以确认的历史价值只有两点：

- 它记录过一次真实的兼容性排障过程
- 它沉淀过若干迁移思路，例如：
  - `uuid` 类型适配
  - axum feature 调整
  - SeaORM / sqlx 边界问题
  - migration API 变更处理

## 未完成

这份文档没有继续维护成“当前状态文档”，因此以下信息都不应再被信任为现状：

- 剩余错误数量
- 预计修复时间
- 当前分支名
- 达到“已完全修复”的判断

## 下一步

1. 如需判断当前编译状态，直接运行 `cargo check`。
2. 如需记录新的 Rust / Cargo 兼容性问题，应新开专项文档，不复用这份旧记录。
3. 当前项目总体状态请以 [`docs/CURRENT_STATUS.md`](./CURRENT_STATUS.md) 为准。
